---
layout: ../../layouts/Post.astro
title: "Running Sum Of 1d Array"
day: 14
date: 2026-06-17
difficulty: easy
topics: [array, prefix sum]
timeComplexity: "O(n)"
spaceComplexity: "O(1)"
---

## Problem

Given an array nums. We define a running sum of an array as runningSum[i] = sum(nums[0]…nums[i]).

Return the running sum of nums.
```
Input: nums = [1,2,3,4]
Output: [1,3,6,10]
```

```
Input: nums = [1,1,1,1,1]
Output: [1,2,3,4,5]
```

## Optimal approach
The optimal approach is to use the prefix sum approach, where the sum of all elements upto that index is
calculated.

## Solution

```java
class Solution {
    public int[] runningSum(int[] nums) {
      int[] arr = new int[nums.length];
      arr[0] = nums[0];
      for(int i = 1; i < arr.length; i++)
      {
        arr[i] = arr[i-1] + nums[i];
      }
      return arr;
    }
}
```

## What I learned

This problem taught me to use the prefix sum technique.